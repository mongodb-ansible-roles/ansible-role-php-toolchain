---
distro_dict:
  Debian: debian
  RedHat: rhel
distro: "{{ distro_dict[ansible_os_family] }}"
architecture_dict:
  x86_64: 64bit
  ppc64le: power8
  s390x: zseries
  aarch64: arm64
architecture: "{{ architecture_dict[ansible_architecture] }}"
